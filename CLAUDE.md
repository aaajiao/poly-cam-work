# CLAUDE.md

This project keeps a **single source of truth** for agent instructions in [`AGENTS.md`](./AGENTS.md)
(shared with Codex, opencode, and other tools). Claude Code reads the file below via the `@` import.

**Do not duplicate guidance here.** Edit `AGENTS.md` instead — this file only re-exports it so the
two never drift.

@AGENTS.md
